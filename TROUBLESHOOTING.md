# TROUBLESHOOTING.md

작업 중 겪은 문제를 그날그날 기록한다. 근거 수치는 실제 실행값만 — 없으면 "미측정".

| 날짜 | 증상 | 원인 진단 | 해결 방법 | 근거 수치 |
|---|---|---|---|---|
| 2026-09-08 | MTTR을 계산했더니 대부분 0시간으로 나옴 | Azure PdM의 `failures`/`maint` timestamp가 일 단위 해상도(대부분 06:00 고정)만 가짐 — 같은 날 수리되면 델타가 0이 됨. 계산 로직 버그가 아니라 원본 데이터의 시간 해상도 한계 | 버그로 보고 로직을 고치는 대신, 실제 델타 분포를 먼저 확인 → 761건 중 743건(97.6%)이 0시간, 18건은 최대 2,883시간(약 120일)까지 실제 지연이 있음을 확인하고 그대로 반영 | MTTR 전체 평균 19.98시간, delta=0 743건/761건, 0이 아닌 최대값 2,883시간 |
| 2026-09-08 | `validate_kpi`가 정상 데이터인데도 "'scenario_id'는 문자열(object)이어야 하는데 str임" 에러를 냄 | pandas 3.0부터 문자열 컬럼의 기본 dtype이 `object`가 아니라 전용 `StringDtype`(`str`)으로 바뀜. `is_object_dtype`만 체크하던 검증 로직이 새 dtype을 놓침 | `is_object_dtype` 또는 `is_string_dtype` 둘 중 하나만 만족해도 통과하도록 수정 | 수정 전 6개 테스트 중 1개 실패(`test_valid_row_passes`) → 수정 후 6/6 통과 |
| 2026-09-08 | `line_sim.py` 1단계 실행 시 `TypeError: float() argument must be a string or a real number, not 'NAType'` | 신뢰구간이 없는 행에 `pd.NA`를 넣고 `.astype("float64")`를 호출 — pandas 3.0에서 `pd.NA`는 float64로 직접 변환이 안 됨 | `pd.NA` 대신 `float("nan")`을 사용하도록 수정 | 수정 후 UPH 59.50(이론값 60.00 대비 0.83% 오차)으로 정상 실행, 재현성(같은 seed) True 확인 |
| 2026-09-08 | `line_sim.py` 2단계에서 병목공정(열처리) MTBF의 3배 기간(272일)을 돌렸는데 Availability(99.92%)가 이론값(calc_availability 98.89%)보다 눈에 띄게 높게 나옴 | 버그 아님 — 272일 동안 열처리 MTBF(2,176h) 기준 기대 고장 횟수는 약 3회인데, 실제로는 1회만 발생(지수분포 특성상 충분히 나올 수 있는 표본 변동, 포아송 근사로 평균 3회일 때 1회가 나올 확률 약 15%). 표본이 작아서 생기는 "1회 실행의 함정" | 로직을 고치지 않고 실제 고장 횟수(공정별)를 verbose 로그로 확인해서 원인이 표본 변동임을 검증 → STEP 7(몬테카를로 반복)의 필요성을 실증하는 근거로 활용 | 272일 시뮬레이션에서 열처리 실제 고장 1회(기대 약 3회), 다운타임 5.36시간; 다른 공정은 가공1 3회/104.61h, 가공2 3회/72.59h, 조립 2회/6.94h, 검사 7회/126.80h |
| 2026-09-08 | `line_sim.py` 3단계에서 `simpy.Store(capacity=0)`을 시도하면 `"capacity" must be > 0` 에러 | SimPy의 Store는 용량 0을 지원하지 않음 — STEP 7 그리드에 buffer_cap=0이 포함돼 있어 그대로 두면 그리드 실행이 죽음 | buffer_caps 값을 `max(1, int(v))`로 클램핑해서 0을 최소 용량 1로 대체. 완전한 "무버퍼 동기식 전달"은 이 프로젝트 범위 밖이라고 문서화 | 클램핑 후 buffer_caps={0}으로 호출해도 정상 실행됨(내부적으로 용량 1로 처리) |
| 2026-09-08 | 8시간(480분) 시뮬레이션에서 buffer_cap을 0→20으로 바꿔도 OEE·UPH가 전혀 안 바뀜(둘 다 0.9917/59.50) | 버그 아님 — MTBF가 수천 시간대라 8시간 안에는 고장이 거의 발생하지 않고, 고장이 없으면 라인이 이미 병목(열처리) 속도로 정확히 동기화되어 버퍼에 재고가 쌓일 일이 없음. 버퍼는 "변동(고장·불량)을 흡수"하는 역할이라 변동이 없으면 크기가 의미 없음 | 로직 수정 없음 — 이건 실제 발견(TOC: 병목은 버퍼가 아니라 열처리 CT). STEP 7 시나리오 그리드 해석 시 이 현상이 재현되면 같은 이유로 설명 가능 | buffer=0: OEE=0.9917, UPH=59.50 / buffer=20: OEE=0.9917, UPH=59.50 (완전히 동일) |
| 2026-09-08 | STEP 7 시나리오 그리드(30개 조합) 전체에서 buffer_cap 0→20 구간의 OEE 개선폭이 전부 0.0으로 나옴. PM 주기는 800·1600분에서 갑자기 Availability가 100%로 튐(400분일 때 93.75%인데) | (1) 버퍼: 위 항목과 동일 이유 — 8시간(480분) 시프트 안에서는 병목공정(열처리, MTBF 2,176h)이 거의 고장 나지 않아 재고 흡수가 필요한 상황 자체가 발생하지 않음. (2) PM: pm_interval(800·1600분)이 sim_time(480분)보다 크면 그 480분 창 안에서 PM이 한 번도 발생하지 않아 Availability=1.0이 되는 게 당연함 — 버그가 아니라 "PM 주기가 관측 기간보다 길면 관측되지 않는다"는 자명한 현상 | 로직 수정 없음. 포트폴리오 문서화 시 이 결과를 "버퍼는 변동 흡수용이라 변동이 없는(=매우 신뢰성 높은 실측 MTBF) 조건에서는 효과가 안 보인다"는 정직한 한계로 기술. 필요하면 향후 버전에서 sim_time을 8시간보다 길게(예: 1주일 교대 누적) 잡아 버퍼 효과를 재현할 수 있음(추가 미착수) | 버퍼 0~20 전 구간 OEE=0.991667(변화 없음); PM 800·1600 Availability=1.0000, PM 400 Availability=0.9375, PM 200 Availability=0.8750; 그리드 30개 시나리오×30회 몬테카를로 총 소요시간 20.3초 |
| 2026-09-08 | STEP 10 준비 중 `kpi.parquet`을 열어보니 같은 행이 2벌씩 중복됨(pdm_cost_sensitive KPI 4종이 8행) | `append_kpi()`가 항상 기존 데이터에 이어붙이기만 하고 중복 제거를 안 함 — 검증 실행(제가 확인차 1회) + 사용자 실행(1회)으로 같은 스크립트가 2번 append됨 | `append_kpi()`에 `drop_duplicates(subset=["scenario_id","source","process","kpi_name"], keep="last")`를 추가해 같은 조합 재실행 시 최신 값으로 덮어쓰도록 수정. 동시에 라인 시뮬레이션 base 시나리오 KPI가 `kpi.parquet`에 아예 없었던 것도 발견해 `build_scenarios.py`에서 buffer_cap=5·pm_interval=0 조합을 "base"로 추가 반영 | 수정 후 kpi.parquet 21행, 중복 0건(duplicated().sum()==0) 확인 |
| 2026-09-08 | `scheduling.py`에서 SPT 규칙의 메이크스팬(1038.6초)이 FCFS(575.9초)보다 오히려 80% 더 나쁘게 나옴 — "SPT가 항상 더 낫다"는 통념과 배치 | 버그 아님 — SPT를 5개 공정 각각에서 독립적으로 적용하면, 특정 잡(Job3·Job5처럼 여러 공정에서 처리시간이 긴 잡)이 매 공정마다 계속 뒤로 밀려 대기시간이 누적된다(현장에서 "장기물 방치" 문제와 동일). SPT는 단일공정 평균흐름시간 최적화에는 이론적으로 우수하지만, 다단계 플로우샵의 메이크스팬 최적화는 보장하지 않는다(Johnson's rule 등 별도 알고리즘 필요) | 로직 수정 없음 — 개별 잡별 시작·종료 스케줄을 출력해 Job3·Job5가 매 공정에서 뒤로 밀리는 패턴을 직접 확인해 원인 규명. 대시보드 페이지에 "SPT가 항상 유리한 건 아니다"라는 해석을 그대로 노출하기로 함 | FCFS 메이크스팬 575.9초 vs SPT 메이크스팬 1038.6초(+80.3%, 시드 42, 6잡×5공정) |
| 2026-09-10 | STEP 11 What-if 페이지에서 병목공정이 "가공1"로 표시됨(실제론 열처리여야 함), 그 여파로 몬테카를로 히스토그램이 n=0회로 비어있음 | `run_monte_carlo()`가 `groupby(["process","kpi_name"])`로 집계하면서 행 순서가 알파벳순으로 바뀌는데, 페이지 코드가 "결과의 첫 행 = 병목공정"이라고 잘못 가정함. mtbf/mttr는 모든 공정에 다 있어서 알파벳상 먼저 오는 "가공1"이 뽑힘 | `oee` kpi_name을 가진 행만 필터링해서 병목공정을 찾도록 수정(oee는 병목공정에서만 생성됨) | 수정 후 buffer=5·pm=0(base) 기준 bottleneck="열처리"로 정상 확인, 히스토그램 n=30 정상 |
| 2026-09-10 | 같은 페이지에서 "캐시 조회"라면서 462ms가 걸림 — 캐시 조회치고 안 빠름 | 캐시 히트 시에도 히스토그램용 개별 실행값(30회 몬테카를로)을 매번 새로 계산하는데, 이 재계산 시간이 "캐시 조회 시간"에 합산되어 표시됨 | 캐시/실시간 판단에 쓰는 시간(`elapsed`)과 히스토그램 재계산 시간(`hist_elapsed`)을 분리해서 각각 측정·표시 | 분리 후 캐시 조회 자체는 2.15ms, 히스토그램 재계산은 별도 0.66초로 정확히 구분 표시됨 |
| 2026-09-10 | STEP 12 QA 중 `fig_gantt`의 y축 제목 "공정"이 PNG에서 "굣0/ㄷ0"처럼 깨짐. `fig_yamazumi`의 "시간 (초)"는 정상 | Kaleido(PNG 내보내기)가 **받침 있는 한글 음절**(공, 정, 병, 목, 동, 률 등)을 Plotly가 기본으로 90도 회전시키는 y축 제목 위치에 그릴 때 글자가 깨지는 렌더링 버그. 받침 없는 음절(시, 간, 가)은 회전해도 정상. go.Bar/px.timeline 등 차트 종류와 무관하게 재현됨(순수 Kaleido/Chromium 폰트 셰이핑 문제) | 회전된 기본 y축 제목을 아예 안 쓰고, `src/viz/common.py`에 `set_horizontal_yaxis_label()`을 만들어 축 제목을 가로 텍스트 주석으로 플롯 영역 왼쪽 위에 표시하도록 전체 차트 함수(fig_oee, fig_quality, fig_line, fig_sim)를 수정 | 수정 전: "공정"→"굣0/ㄷ0"로 깨짐(고해상도 scale=3에서도 재현). 수정 후: 8개 PNG 전체 육안 확인, 받침 있는 한글 전부 정상 렌더링 |
| 2026-09-10 | `fig_monte_carlo`가 8시간 base 시나리오(30회 전부 동일값 0.991667)에 대해 히스토그램을 그리자, 막대가 플롯 전체 폭을 가득 채우는 의미 없는 그림이 됨 | Plotly Histogram이 분산이 0인 데이터(모든 값이 동일)에 대해 구간(bin) 폭을 정하지 못해 자동으로 넓은 범위를 하나의 막대로 채움 — 버그라기보다 이 데이터가 "변동이 아예 없다"는 걸 시각화하기에 일반 히스토그램이 부적합했던 것 | 값 범위가 0에 가까우면(모든 실행값 동일) 히스토그램 대신 단일 스파이크 막대 + "n회 모두 동일한 값 — 변동 없음" 텍스트로 명시적으로 표시하도록 `fig_monte_carlo`에 분기 추가 | 수정 후 base 시나리오(n=30, 전부 0.991667)는 좁은 스파이크로, 장기 시나리오(n=30, 평균 0.9537, 실제 분산 있음)는 정상 히스토그램으로 각각 올바르게 표시됨 |
| 2026-09-10 | STEP 12 QA에서 페이지별 로딩 시간을 재보니 `1_설비예지보전.py`가 10.27초로 목표(3초)를 크게 초과 | 센서 열화 계산(`load_sensor_precursor`)이 고장 761건마다 telemetry 876,100행 전체를 boolean 마스크로 훑음(O(고장수 × 전체행수)) | machineID로 미리 그룹핑해서 각 조회가 해당 설비 몫(평균 약 8,760행)만 보도록 수정 — 그룹핑은 1회만 하고 재사용 | 최적화 전 10.27초 → 최적화 후 2.19초(페이지 전체), 계산 결과값은 동일(volt 176.169, rotate 417.220, pressure 105.576, vibration 42.909 — 변화 없음, 속도만 개선) |
| 2026-09-10 | STEP 13 배포 준비 중 `data/raw/`를 저장소에서 빼려고 보니, `1_설비예지보전.py`가 배포 환경에서 바로 깨질 뻔함 | `load_failures()`와 `load_sensor_precursor()`가 앱 실행 중에 `data/raw/azure_pdm/PdM_telemetry.csv`(80MB)를 직접 읽고 있었음 — 로컬에선 원본이 있어 문제없었지만, "원본은 읽기 전용 + 용량 때문에 저장소에서 제외"라는 STEP 13 원칙과 정면충돌 | 두 계산을 `src/kpi/precompute_page1.py`로 옮겨 `data/interim/failure_pareto.csv`, `data/interim/sensor_precursor.parquet`로 미리 저장해두고, 페이지는 그 결과만 읽도록 수정. 다른 KPI(mtbf_mttr 등)와 동일한 "미리 계산해서 interim에 저장" 패턴으로 통일됨 | 수정 전후 계산값 100% 동일(volt 176.169/rotate 417.220/pressure 105.576/vibration 42.909, 고장건수 comp2 259·comp1 192·comp4 179·comp3 131) 확인 후 페이지 교체. 배포에 필요한 데이터 총량이 data/raw 131MB → data/interim+processed+legacy 약 116KB로 감소, Git LFS 불필요 |
| 2026-09-10 | Streamlit Community Cloud 실제 배포 후 QA: `2_품질SPC.py`가 `ImportError`로 전체 페이지 다운 | `pd.read_excel(..., engine="openpyxl")`을 쓰는데, requirements.txt를 배포용으로 최소화하면서 `openpyxl`을 빠뜨림 — 로컬 `.venv`엔 이미 설치돼 있어서(다른 이유로 이전에 설치됨) 로컬 테스트로는 이 누락을 못 잡았음 | requirements.txt에 `openpyxl` 추가 후 재배포. 재발 방지로 app/·src/의 전체 import 문을 다시 스캔해 requirements.txt와 대조 확인 | 재배포 후 품질SPC 페이지 정상 로딩 확인 |
| 2026-09-10 | 같은 배포 QA에서 `5_WhatIf시뮬레이터.py`의 몬테카를로 분포 차트(변동 없음 케이스)에서 "30회 모두 동일한 값(...)" 안내 문구가 스파이크 막대와 겹쳐 보임 | `fig_monte_carlo()`의 degenerate-case 분기가 안내 문구를 플롯 영역 정중앙(paper 좌표 x=0.5, y=0.5)에 고정해뒀는데, 스파이크 막대 높이(=반복 횟수 n)가 y축 범위의 상당 부분을 차지해서 문구와 막대가 겹침 | 문구 위치를 막대 높이와 무관하게 항상 플롯 영역 "위쪽 바깥"(y=1.08, yanchor="bottom")으로 고정 | 로컬에서 n=30·동일값 0.9312로 재현 후 PNG로 겹침 사라짐을 육안 확인 |
