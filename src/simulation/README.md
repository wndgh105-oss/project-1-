# src/simulation/

SimPy 기반 가상 생산라인(디지털트윈)과 몬테카를로/시나리오 계산.

이 프로젝트는 기존 sim1~sim5 자산을 재활용하지 않고 **처음부터 새로 설계**한다
(2026-09-08 결정 — 해당 자산이 이 PC에 없었음).
예: `line_sim.py`(STEP 6), `monte_carlo.py`·`build_scenarios.py`(STEP 7).
